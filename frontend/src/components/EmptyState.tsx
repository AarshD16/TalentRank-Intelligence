export default function EmptyState() {
  return (
    <section className="empty-state">
      <span className="eyebrow">Ready for a run</span>
      <h2>Upload a JD and candidates to launch the ranking pipeline.</h2>
      <p>The dashboard will show progress, evidence coverage, RoleSpec policy details, and CSV exports after completion.</p>
    </section>
  );
}
